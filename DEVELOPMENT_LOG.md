# 2026GameServerTermproject 개발 일지 (PROGRESS)

## 📌 프로젝트 개요
*   **목표:** 고성능 다중 접속 MMORPG 서버 제작 (IOCP 기반, 동접 5000명 목표)
*   **주요 요구사항:** 
    *   2000x2000 맵 및 20x20 시야 범위 처리 (장애물 포함)
    *   20만 마리 NPC (Peace / Agro 등 상태 및 로밍)
    *   DB 연동 및 스크립트 데이터 로드
    *   A* 알고리즘 길찾기
*   **⚠️ 핵심 제약사항:** 실습 코드(`npc.cpp` 등) 그대로 사용 불가 (표절 감점 방지)

---

## ✅ 개발 진행 상황 (완료된 작업)

### 1. 프로젝트 요구사항 및 현황 분석 완료
*   기획서(PDF) 요구사항과 현재 작성된 `MMOSERVER_Termproject.cpp`, `npc.cpp` 구조 대조.
*   기존 실습 코드(`npc.cpp`)의 구조(prev_recv 패킷 조립, 2차원 배열 g_sectors 등)를 그대로 복사할 경우 표절 감점 위험이 매우 높음을 확인.
*   이에 따라 게임 서버의 핵심 기반(네트워크 수신, 시야, 타이머 등)을 백지상태에서 새로운 객체지향/모던 C++ 구조로 재설계하기로 결정.
*   클라이언트 및 스트레스 테스트 코드는 서버 프로토콜 뼈대가 완성된 후 보정 작업만 진행하기로 합의. 비트맵 아트워크는 파이썬 절차적 생성이나 에셋 사이트 이용 방향으로 결정.

### 2. 독자적인 네트워크 수신부 (Ring Buffer) 구현 완료
*   **변경 대상:** `MMOSERVER_Termproject.cpp`
*   **내용:** 실습 코드의 포인터 이동 방식(`prev_recv`) 패킷 재조립을 제거하고, 원형 큐 형태의 `RingBuffer` 클래스를 직접 설계하여 도입.
*   **장점:** TCP 스트림의 고질적 문제인 패킷 단편화(잘려서 옴) 및 뭉침(여러 개가 한 번에 옴) 현상을 아주 안전하고 명시적으로 처리할 수 있게 됨. 표절 의심을 원천 차단함.

### 3. 클라이언트 테스트 환경 프로토콜 동기화 완료
*   **변경 대상:** `CLIENT/client_sample/client.cpp`
*   **내용:** 클라이언트의 헤더 참조 경로를 구버전(`protocol.h`)에서 최신 버전(`protocol_2026.h`)으로 수정.
*   클라이언트의 이동 패킷(`C2S_MOVE`) 송신 방식을 Enum 방향 값 전송에서 목표 좌표(x, y) 전송으로 리팩토링.
*   서버의 통합 객체 패킷(`S2C_ADD_OBJECT`, `S2C_MOVE_OBJECT`, `S2C_REMOVE_OBJECT`)을 클라이언트가 올바르게 수신 및 파싱하여 렌더링하도록 로직 수정.
*   향후 서버 개발 과정에서 즉시 클라이언트를 띄워 시각적인 테스트가 가능한 환경을 구축.

---

## 🚀 향후 진행 계획 (Next Steps)

1.  **공간 분할(Spatial Partitioning) 및 시야 관리 로직 설계**
    *   단순 2차원 배열 `g_sectors` 방식 대신, 객체 지향적인 `Grid` 또는 `QuadTree` 기반의 시야 관리 클래스 제작.
2.  **커스텀 Timer Manager 구현**
    *   TBB `concurrent_priority_queue`에 의존하던 실습 방식 대신, `std::priority_queue`와 `std::condition_variable`을 활용한 전용 Timer 스레드(Dispatcher) 구성.
3.  **NPC AI (FSM) 및 길찾기 (A*) 도입**
4.  **ODBC 비동기 DB 연동 및 JSON/Lua 스크립트 기반 데이터 로딩**
